services:
    quakejs:
        container_name: quakejs
        build:
            context: .
            dockerfile: Dockerfile
        image: treyyoder/quakejs:latest
        restart: unless-stopped
        environment:
            HTTP_PORT: "8080"
        ports:
            - "8080:80"
            - "27960:27960"
